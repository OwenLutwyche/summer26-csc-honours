# src/scanpy/preprocessing/__init__.codon

# 1. Import kernels and rename them to API names
from .kernels import (
    log1p_kernel as log1p,
    normalize_total_dense_kernel as normalize_total_dense,
    normalize_total_sparse_kernel as normalize_total_sparse,
    filter_cells_dense_kernel as filter_cells_dense,
    filter_genes_dense_kernel as filter_genes_dense,
    filter_cells_sparse_kernel as filter_cells_sparse,
    filter_genes_sparse_kernel as filter_genes_sparse,
    sqrt_kernel as sqrt,
    regress_out_kernel as regress_out
)

from ._pca import pca
from ._scrublet import scrublet


# 2. Import Scale
from ._scale import scale_dense_kernel as scale_dense
from ._scale import scale_sparse_kernel as scale_sparse

# 3. Import Highly Variable Genes
from ._highly_variable_genes import highly_variable_genes_seurat_dense

from ._qc import calculate_qc_metrics

# 4. Explicitly export (Standard Python practice that helps Codon too)
__all__ = [
    "pca",
    "scrublet",
    "calculate_qc_metrics",
    "log1p",
    "normalize_total_dense",
    "normalize_total_sparse",
    "filter_cells_dense",
    "filter_cells_sparse",
    "filter_genes_dense",
    "filter_genes_sparse",
    "sqrt",
    "scale_sparse",
    "scale_dense",
    "highly_variable_genes_seurat_dense",
    "regress_out",
]

